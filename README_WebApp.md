# FreeAnime WebApp - Документация

## 🎯 Обзор

WebApp интерфейс предоставляет современный способ взаимодействия с ботом FreeAnime через Telegram Web Apps. Пользователи могут искать аниме, выбирать озвучку и смотреть видео в удобном веб-интерфейсе.

## 📁 Структура файлов

```
webapp/
├── mainWebApp.html       # Главная страница поиска аниме
├── animeDetails.html     # Детали аниме и выбор озвучки/серий
├── checkPleer.html       # Видеоплеер с навигацией
└── README_WebApp.md      # Эта документация
```

## 🌐 Основные функции

### 🔍 Поиск аниме (`mainWebApp.html`)
- **Поле поиска** с плейсхолдером "Введите название аниме..."
- **Кнопка поиска** с иконкой лупы
- **Результаты поиска** в виде карточек с постерами
- **Информация о аниме**: название, оригинальное название, год, тип (фильм/сериал)

### 📋 Детали аниме (`animeDetails.html`)
- **Постер и информация** об аниме
- **Выбор озвучки** из доступных вариантов
- **Выбор серии** (только для сериалов)
- **Кнопка "Смотреть"** для запуска воспроизведения

### 🎬 Видеоплеер (`checkPleer.html`)
- **HTML5 видео плеер** с Plyr.js
- **Информация о видео**: название, озвучка, серия
- **Управление сериями**: предыдущая/следующая серия
- **Горячие клавиши**:
  - `Пробел` - пауза/воспроизведение
  - `← →` - навигация по сериям
  - `F` - полноэкранный режим

## 🎨 Дизайн и UX

### 🌈 Темная/Светлая тема
- Автоматическая адаптация под тему Telegram
- Использование CSS переменных:
  ```css
  --tg-theme-bg-color              /* Фон */
  --tg-theme-text-color            /* Текст */
  --tg-theme-button-color          /* Кнопки */
  --tg-theme-secondary-bg-color    /* Вторичный фон */
  --tg-theme-hint-color            /* Подсказки */
  ```

### 📱 Адаптивность
- **Мобильная оптимизация** для Telegram Web Apps
- **Безопасные отступы** для различных устройств
- **Touch-friendly** интерфейс

### ⚡ Производительность
- **Ленивая загрузка** изображений
- **Минимизированные анимации**
- **Оптимизированные запросы** к API

## 🔧 Техническая реализация

### 📡 Взаимодействие с ботом

WebApp отправляет данные боту через `Telegram.WebApp.sendData()`:

```javascript
// Поиск аниме
window.Telegram.WebApp.sendData(JSON.stringify({
    action: 'search',
    query: 'Атака титанов'
}));

// Выбор аниме
window.Telegram.WebApp.sendData(JSON.stringify({
    action: 'select_anime',
    animeId: '123'
}));

// Просмотр аниме
window.Telegram.WebApp.sendData(JSON.stringify({
    action: 'watch_anime',
    animeId: '123',
    translationId: '456',
    episode: 1
}));
```

### 🎯 Обработка действий в боте

Бот обрабатывает WebApp данные в `tgBot.py`:

```python
@dp.message(F.web_app_data)
async def handle_webapp_data(message: Message):
    data = json.loads(message.web_app_data.data)

    if data['action'] == 'search':
        # Обработка поиска
    elif data['action'] == 'select_anime':
        # Обработка выбора аниме
    elif data['action'] == 'watch_anime':
        # Запуск просмотра
```

## 🚀 Развертывание

### 1. GitHub Pages
Загрузите файлы из папки `webapp/` в корень репозитория `asyabondi.github.io`:

```
asyabondi.github.io/
├── mainWebApp.html
├── animeDetails.html
├── checkPleer.html
└── README_WebApp.md
```

### 2. Проверка работы
- `https://asyabondi.github.io/mainWebApp.html` - Поиск
- `https://asyabondi.github.io/animeDetails.html?animeId=1` - Детали
- `https://asyabondi.github.io/checkPleer.html` - Плеер

## 🐛 Отладка

### Инструменты разработчика
1. **Chrome DevTools** - для отладки JavaScript
2. **Telegram Web Inspector** - для тестирования WebApp
3. **Логи бота** - для отслеживания запросов

### Распространенные проблемы

#### WebApp не открывается
```javascript
// Проверьте инициализацию
window.Telegram.WebApp.ready();
console.log('WebApp version:', window.Telegram.WebApp.version);
```

#### Данные не отправляются
```javascript
// Проверьте формат данных
const data = JSON.stringify({action: 'test'});
window.Telegram.WebApp.sendData(data);
```

#### Стили не применяются
```css
/* Проверьте CSS переменные */
body {
    background: var(--tg-theme-bg-color, #fff);
    color: var(--tg-theme-text-color, #000);
}
```

## 📈 Метрики и аналитика

### Производительность
- **Время загрузки** страниц
- **Размер бандла** (минифицированный HTML/CSS/JS)
- **Количество запросов** к API

### UX метрики
- **Конверсия** поиск → выбор → просмотр
- **Время сессии** в WebApp
- **Частота использования** различных функций

## 🔮 Будущие улучшения

### Планируемые функции
- [ ] **Избранное** - сохранение любимого аниме
- [ ] **История просмотров** - отслеживание прогресса
- [ ] **Рекомендации** - персонализированные предложения
- [ ] **Оффлайн режим** - кэширование видео
- [ ] **Качество видео** - выбор разрешения

### Технические улучшения
- [ ] **PWA поддержка** - установка как приложение
- [ ] **WebRTC** - пиринговое распространение
- [ ] **WebGL** - аппаратное ускорение
- [ ] **Service Workers** - кэширование ресурсов

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота в терминале
2. Откройте DevTools в браузере
3. Проверьте документацию по Telegram Web Apps
4. Создайте issue в репозитории проекта

---

**Версия:** 1.0.0
**Дата:** 12 января 2026
**Автор:** FreeAnime Team